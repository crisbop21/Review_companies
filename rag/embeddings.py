"""Embedding backfill. Implementation lands in the RAG enablement phase."""


def embed_chunk(text: str) -> list[float]:
    raise NotImplementedError("RAG is disabled until embeddings are enabled.")
